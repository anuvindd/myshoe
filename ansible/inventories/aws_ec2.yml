plugin: aws_ec2
regions:
  - ap-south-1
filters:
  tag:Project: myshoe
  instance-state-name: running
keyed_groups:
  - key: tags.Role
    prefix: role
hostnames:
  - private-ip-address
compose:
  ansible_host: public_ip_address
